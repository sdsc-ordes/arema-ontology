name: Generate Public Ontology Docs

on:
  push:
    paths:
      - 'src/ontology/arema-ontology.ttl'
      - '.github/workflows/docs.yml'

jobs:
  build-docs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare .env
        run: echo "BASEURL=/$(basename $GITHUB_REPOSITORY)" > .env

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build docs in container
        run: |
          mkdir -p public docs

          # Mount src/ontology as /app/data inside container
          CONTAINER_ID=$(docker create \
            -v $(pwd)/src/ontology:/app/data \
            -v $(pwd)/.env:/app/.env \
            ghcr.io/sdsc-ordes/skohub-vocabs:latest
          )

          docker start -ai $CONTAINER_ID
          mkdir -p docs
          # Copy output
          docker cp $CONTAINER_ID:/app/public/. ./docs
          docker rm $CONTAINER_ID



      - name: Commit updated docs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs
          git diff --cached --quiet || git commit -m "Update docs via skohub-vocabs"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
